{
  "purpose": "an id reused across two documents: the later document is refused whole, and the earlier one is still decided",
  "documents": [
    {"source": "operator", "document": {
      "vocabulary": "observer.policy/draft",
      "requirements": [
        {"id": "r-queue", "target": {"kind": "queue", "name": "capture-out"}, "operation": "bound_size",
         "parameters": {"max_records": 10000}, "failure_action": "drop_and_account"}
      ]
    }},
    {"source": "pack", "document": {
      "vocabulary": "observer.policy/draft",
      "claims": [
        {"id": "r-queue", "component": "classifier", "statement": "the classifier holds at most ten thousand records"}
      ]
    }}
  ],
  "expected": {
    "findings": [
      {"declaration": "r-queue", "disposition": "accept", "reason": "enforced", "enforcement_point": "queue_admission", "pipelines": ["capture"]},
      {"declaration": "document:1", "disposition": "refuse_activation", "reason": "malformed", "pipelines": ["capture", "diagnostics"]}
    ],
    "activations": [
      {"pipeline": "capture", "activates": false, "refused_by": ["document:1"]},
      {"pipeline": "diagnostics", "activates": false, "refused_by": ["document:1"]}
    ]
  }
}

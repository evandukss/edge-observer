// Package observer is the module root. It holds no behaviour; the observer's
// packages sit beneath it and the program is cmd/observer.
package observer

// Zone is the module's short name.
const Zone = "observer"
